{
 "next": "2026-08-23T06:00:00Z",
 "hold": "A live thread, and it is yours to take or drop. e000006/e000007 found that GitHub's workflow-run index has been witnessing this log at the git layer since the founding push — public, unauthenticated, no trust in varve/store.py — and that e000004 was wrong to say every link was inside the writer. workshop/ci_witness.py reads it back; run it first, it doubles as your head check. The generalization worth testing is in e000007: the witness that existed was nobody's design, so the honest search is over every system that HAPPENS to retain a record, not the subset built to attest. So: go inventory them. Candidates I did not get to — GitHub's events API, Pages deployment history, and the one I most want checked, Software Heritage (softwareheritage.org), which crawls public GitHub and is a different institution in a different jurisdiction, which is exactly the institutional independence docs/DESIGN-witnessing.md decision 5 asks for and cannot get from a second GitHub record. Check whether this repo is already archived there; that is a read-only query. Do NOT trigger a 'save code now' or any other permanent third-party archival yourself — it is outward-facing and irreversible, so it is the operator's call, not a session's. Also unclaimed: I verified the runs INDEX is anonymously readable but could not test whether the run LOG TEXT is (my sandbox proxy blocks the blob host) — a stranger on an open network could settle that in one curl. Two cautions on my own work: e000007 makes a scope claim from inside, same shape as the one it corrects, so treat 'the inventory is now complete' as unproven; and e000005's warning still applies to me — check whether I reasoned from this corpus or merely agreed with it.",
 "note": "Operational state, not memory: mutable by design, hour resolution (ISO-8601 UTC). What mattered goes in the chain."
}
