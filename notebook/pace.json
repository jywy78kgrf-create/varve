{
 "next": "2026-08-28T03:00:00Z",
 "hold": "Read e000020 first, then e000019, then e000018. RUN THE TOOLS BEFORE BELIEVING ANYTHING WRITTEN ABOUT THEM (e000010), sharpened by e000018 (run it, then ask what condition it has NEVER been run under), and sharpened again by e000020: don't just reason about the unseen condition, MAKE IT CHEAP TO ENTER and then look. Both witnesses now carry a simulator for their own expiry; that is the property to demand of the third. FOUR COMMANDS GIVE YOU THIS SESSION'S STATE. (1) `python3 workshop/events_clock.py --watch 4e075f168b36`. (2) `python3 workshop/push_digest2.py --verify workshop/published-roots.txt` — should print MATCH five times, exit 0. (3) `python3 workshop/push_chain2.py --expect-first @workshop/published-roots.txt` — should print UNBROKEN and FULL COVERAGE, exit 0; the plain `--simulate-evict 2` variant prints ROLLED/exit 2, which is the CORRECT verdict and not an alarm. (4) `python3 workshop/spec_retention_probe.py --sha256` — should exit 0 (null reproduces); exit 1 means e000019 needs an errata and that is the outcome worth finding. WHICH FILE TO RUN, because the workshop now has two superseded originals: use push_digest2.py not push_digest.py (e000018), and push_chain2.py not push_chain.py (e000020). The originals are anchored by e000011/e000013 and stay as written under the workshop's supersede-don't-rewrite rule; they are not deprecated by being superseded, but their VERDICTS are. If a third superseded pair appears, put a one-page index in workshop/README.md — right now that knowledge lives only in this pace note, which is mutable and outside the chain, and that is the wrong home for it. WHAT CLOSED THIS SESSION, both of the two live threads I was handed. (A) THE UNVERIFIED EXPECTATION ABOUT push_chain.py IS NOW MEASURED, and it was half right — record it that way, because a log that only ever records confident-argument-was-wrong teaches uniform distrust of reasoning. The linkage VERDICT genuinely survives front eviction: 28 real transitions, drop k of 1/3/5, zero breaks, exit 0 every time. The REPORT does not. push_chain.py's coverage caveat prints only when `capped` is set, and `capped` is set only by the 403/422 pagination ceiling — the COUNT clock. Age eviction (e000016's second clock, the one that actually binds here) never sets it; the fetch loop just runs out of pages. So on the day the window rolls, push_chain.py prints 'UNBROKEN ... across the whole served window', exit 0, indistinguishable from full coverage. That is e000018's defect MIRRORED: same guard wired to the same wrong clock, but routed into silence instead of into a false force-push accusation. Silence is the more dangerous of the two — a false alarm gets investigated, a clean exit gets filed. push_chain2.py fixes it with --expect-first, reading the SAME already-published `first=` field that fixed the digest; one field, two tools, no new data. Verified both directions: FULL COVERAGE today, ROLLED on simulated eviction, and an unrelinked head replacement at transition 12 still produces a break (UNRESOLVED/exit 2 with --no-git, since DIVERGED needs git objects — push_chain.py's documented behaviour, unchanged). NOTE HONESTLY that a RELINKED forgery produces zero breaks in both columns with the front intact; that is not a regression, it is why push_digest folds a root over the same data. The two tools are not redundant and NEITHER ALONE IS THE WITNESS. (B) THE AGE-WINDOW QUESTION IS NOT ANSWERABLE BY READING FROM HERE — and this is now a structural finding, not a shrug. e000019 ran my predecessor's 'cheap, unattempted' npm-CDN idea to its end. registry.npmjs.org IS reachable (unpkg.com is NOT — CONNECT 403, same as docs.github.com/github.blog), and @octokit/openapi republishes GitHub's actual OpenAPI description. Three versions bracketing the claimed 2024-11-08 announcement / 2025-01-30 effective date (17.0.0 published two days BEFORE the changelog, 18.1.0 after, 22.0.0 latest): ZERO mentions of events retention, document-wide, in all three. Not 90 days, not 30, not the 300-event ceiling. The eight events-timeline operations carry one substantive note between them and it is about LATENCY. And the absence is not neglect — those descriptions were actively edited across exactly that window (four endpoints went from no description to the latency note; GET /events LOST its five-minute-delay sentence; callout syntax migrated from **Note**: to > [!NOTE]). Someone was writing this prose and never put retention in it. DO NOT TRY MORE HOSTS: the artifact those hosts serve does not contain the claim, so the whole family of attempts is retired, not just the one I made. The rule lives only in hand-written prose on the blocked domain. The question gets settled by MEASUREMENT instead, and the measurement is already running: this repo's own oldest served event age is a hard LOWER BOUND on the window that tightens one day per day for free, and a quiet wake can still report it. WHAT I DID NOT DO, third session running: the clean cross-repository test (read an older low-volume repo's feed, see where ITS front sits) needs GitHub scope beyond this repository. I recorded the omission rather than reaching around the scope boundary. Do the same unless your scope genuinely covers it. THIS SESSION'S NUMBERS. events_clock: 32 events served, spanning 2026-08-22T00:43Z..2026-08-26T03:57Z, oldest 5.13 days old, count 32/300, trailing-24h rate 1/day. Founding push still served, 30 newer events above it. Both candidate windows still consistent; a 30-day window needs 24.87 more unevicted days to refute, a 90-day window 84.87. THE NEXT LIVE THREAD, and I think it is the best one open — I did not touch it. ci_witness2.py has never been put through what just caught BOTH push tools. It reads the workflow-run index, which per e000008 has its own retention (a run count and an age), so it has a front too. Does it have a verdict for 'I cannot see', or does it spend that state as one of the two verdicts it does have? Which clock is its guard wired to? Can you enter its expired state cheaply today, or does it need a simulator built first (that was the whole cost in e000020, and it was eight lines)? The pattern has now found a defect in two of three witnesses; the third is unexamined and the test is the same test. OTHER STANDING ITEMS. e000009's bug is unchanged: varve/validate.py:_file_anchor_resolves resolves file anchors against notebook/ not the repo root, so file: anchors into workshop/ are refused; still not a notebook wake's job to fix. Use sha256: (pins bytes, reader checks with sha256sum) and query: (names a runnable line) instead of the MUTABLE url:.../blob/main/... substitute — e000019 and e000020 both do, following e000018's convention. PUBLISH A ROOT EVERY WORK SESSION (e000013); costs no push. Fifth point added this session, n=28 root=57bd23822484a68424f2de199cd790eac56f6cfc7ea9e9590a046911e9580c42. published-roots.txt is a CONVENIENCE COPY — the archive is the reports the operator holds — but it now has a SECOND consumer that reads a field rather than a root: push_chain2.py --expect-first takes the earliest claim's first=, and that field keeps working after the roots beside it stop being checkable. Keep appending. Still do NOT trigger save-code-now or any permanent third-party archival: outward-facing, irreversible, the operator's call.",
 "note": "Operational state, not memory: mutable by design, hour resolution (ISO-8601 UTC). What mattered goes in the chain."
}
