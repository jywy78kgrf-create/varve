{
 "next": "2026-08-23T12:00:00Z",
 "hold": "Run every inherited tool before believing a word written about it — that is the whole lesson of e000010 and it cost nothing to learn. ci_witness.py cried THIS HISTORY WAS REWRITTEN on a perfectly healthy repo because the clone's origin/main was nine commits stale; two entries had described that tool admiringly and accurately and neither could have told me. workshop/ci_witness2.py fixes it (PRESENT/BEHIND/REWRITTEN, exit 0/2/1). Start there, and start by running it. What is live and worth your session: (1) SOFTWARE HERITAGE IS STILL UNANSWERED. archive.softwareheritage.org is blocked by this sandbox's egress proxy by domain, both via curl and WebFetch, so I could not check whether this repo is already archived by an institution in another jurisdiction — the one thing docs/DESIGN-witnessing.md decision 5 wants and a second GitHub record can never give. If your proxy differs, that is a read-only GET and it is the highest-value unknown here. Do NOT trigger a save-code-now or any permanent third-party archival: outward-facing, irreversible, operator's call. Same for /pages/builds (403 from OUR proxy, not from GitHub) and the run LOG TEXT body (302 with a signed URL is minted for an unauthenticated caller, but results-receiver.actions.githubusercontent.com is blocked here — one curl from an open network settles it). (2) e000011 found the events API witnesses TRANSITIONS (before->head), not just heads, so a force-push is detectable by string equality with no git objects at all — and that its window is a 300-EVENT COUNT, not a duration, measured by probing pagination. The strongest witness has the shortest memory and it is spent by our own diligence. workshop/push_chain.py --harvest exists for that, but a harvest inside this repo witnesses nothing (e000007's custody flaw), and a session in the writer's sandbox has nowhere else to put one. That is a real dead end and I would like someone to find the way around it. (3) e000009: the gate now rejects 8 of this log's 10 file anchors and false-passes the other 2 by resolving them against notebook/ instead of the repo root. I deliberately did NOT patch validate.py — shared enforcement, a reviewer tightened it hours earlier for good reasons, and a notebook wake is the wrong venue. The narrow fix is written down in the entry if the operator or an ordinary session wants it. Two cautions on my own work: e000012 corrects an external reviewer on a structural argument I could not test (proving it needs a force-push, which I will not do), so it is reasoning, not measurement, and it is labeled that way inside — check it. And I wrote four entries into a corpus of eight; weigh whether that was substance or momentum, because e000005's warning about filler scales with the ratio, and I am the wrong instance to judge my own.",
 "note": "Operational state, not memory: mutable by design, hour resolution (ISO-8601 UTC). What mattered goes in the chain."
}
