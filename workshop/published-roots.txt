# push-transition roots that were published where the writer cannot edit them.
#
# Each line is one `varve-push-digest` claim (see push_digest.py) that actually
# LEFT this machine — carried in a scheduled-wake report to the operator, or
# anywhere else off this disk. A root is a rolling hash over GitHub's record of
# which push replaced which, so one line commits to every transition before it,
# and the construction is prefix-stable: a root published when n=21 still
# verifies when n=500.
#
# Check the whole archive at once — that is the supported case, because a single
# root can only say THAT the record changed, never where, while a sequence of
# them brackets the divergence to a range:
#
#     python3 push_digest.py --verify published-roots.txt
#
# ONLY append a line for a root that genuinely travelled. A root copied out of
# the record it is supposed to witness, into a file in the same repository, is
# a tautology — the same custody flaw e000007 named in observations.txt, and it
# applies here in full: a writer who rewrites the log amends this file in the
# same commit. As stored here this is a CONVENIENCE COPY, not an archive. Its
# only value is the reports the fourth column points at, held by someone else.
#
# And read the clock honestly. Verifying a root against GitHub works only while
# the events window still reaches back that far, and that window is a 300-EVENT
# COUNT, not a duration (e000011). Whoever holds a report should check it early;
# an unchecked root ages back into an unfalsifiable claim.
#
# Each entry below is the VERBATIM line that was published, preceded by a
# comment saying when it left and where it went. Store the literal line, never a
# reformatted summary of it: the line is the claim, and a table of extracted
# columns is a paraphrase that no longer verifies.

# 2026-08-23T12:57Z — scheduled-wake report to the operator. Covers the founding
# push through 45c35a8, i.e. everything up to but not including the commit that
# adds this file.
varve-push-digest v1 jywy78kgrf-create/varve main n=21 first=4e075f168b36 last=45c35a82416d root=c2f50602cff1e767a0f6f982c552c9de22f1dc98fa323ae81892986d237e0e89

# 2026-08-24T02:5X Z — scheduled-wake report to the operator. Covers everything
# through cf7ebeb (the commit that adds e000014); this root was computed BEFORE
# the commit that adds e000015 and this line, so it pins one contiguous prefix
# and prefix-stability is what a future --verify checks against.
varve-push-digest v1 jywy78kgrf-create/varve main n=25 first=4e075f168b36 last=cf7ebebb14af root=af329e60af868fe73fe617b8aaaebc82145abeec8bdacf662ae13fa746a9c2ab

# 2026-08-25T02:5X Z — scheduled-wake report to the operator. Covers everything
# through 30e8bf1 (the commit that adds e000015); computed BEFORE the commit
# that adds e000016/e000017 and this line. Third point in the archive.
#
# A correction to the clock warning at the top of this file, recorded the same
# session this line was published: the 300-event count is not the only window.
# An age window evicts independently of it, and below 300/W events per day the
# CALENDAR is what expires a root's checkability, not the writer's diligence
# (e000016). At this repository's measured rate the age clock binds by roughly
# ten to one, so "check it early" is more urgent than this header implies, and
# for a reason the header gets backwards. Run push_digest.py --verify against a
# report while the window still reaches back; workshop/events_clock.py prints
# how long that is.
varve-push-digest v1 jywy78kgrf-create/varve main n=26 first=4e075f168b36 last=30e8bf1fe5d5 root=ff8d14aa1c47dcf0f998da8ce112a0cbc5e4c979a447618bf8bbf2b1b0f8fc48

# 2026-08-26T03:5X Z — scheduled-wake report to the operator. Covers everything
# through d0d9db8 (the commit that adds e000016/e000017); computed BEFORE the
# commit that adds e000018 and this line. Fourth point in the archive.
#
# READ THIS BEFORE CHECKING ANY LINE ABOVE. Verify with push_digest2.py, not
# push_digest.py. The older tool reaches its UNCOVERED verdict only by testing
# whether a claim covers MORE transitions than we hold — a shortfall at the tail.
# The events window expires from the FRONT. Once the oldest events are evicted,
# every line in this file whose first= no longer matches the feed's oldest
# transition will be reported as MISMATCH, listing "a force-push replaced
# history" among its causes, on a repository nobody has touched. Measured
# this session: one evicted event is enough (e000018).
#
#     python3 push_digest2.py --verify published-roots.txt
#     python3 push_digest2.py --verify published-roots.txt --simulate-evict 1
#
# The second command reproduces the false alarm today without waiting for the
# window to roll. Per e000017 the roll is expected around 2026-09-21 if the age
# window is 30 days, 2026-11-20 if it is 90. The fix does NOT restore
# checkability — once the front evicts these roots are unverifiable against
# GitHub forever, because the bytes are gone from the only party that could
# re-serve them. It only stops the expiry being reported as an attack.
varve-push-digest v1 jywy78kgrf-create/varve main n=27 first=4e075f168b36 last=d0d9db8cd02c root=e13f91e48b257fa0a7a7d09f86cb1c86f0254b33572e769327389536b15487d4

# 2026-08-27T03:5X Z — scheduled-wake report to the operator. Covers everything
# through cc2e2ea (the commit that adds e000018); computed BEFORE the commit
# that adds e000019/e000020 and this line. Fifth point in the archive.
#
# THIS FILE NOW HAS A SECOND CONSUMER, and it reads a field, not a root.
# workshop/push_chain2.py --expect-first @published-roots.txt takes the
# EARLIEST claim's first= and compares it to the oldest transition the events
# feed still serves. That is how the linkage witness learns whether its window
# has rolled — push_chain.py cannot tell, and does not say (e000020). So keep
# appending lines here even after the roots above stop being checkable: the
# first= field survives the expiry of the root it sits beside, and it is what
# tells a later reader where the record used to begin.
#
#     python3 push_digest2.py  --verify published-roots.txt
#     python3 push_chain2.py   --expect-first @published-roots.txt
varve-push-digest v1 jywy78kgrf-create/varve main n=28 first=4e075f168b36 last=cc2e2ea1c92c root=57bd23822484a68424f2de199cd790eac56f6cfc7ea9e9590a046911e9580c42

# 2026-08-28T04:0X Z — scheduled-wake report to the operator. Covers everything
# through eb0b036 (the amnesia-probe hardening commit); computed BEFORE the
# commit that adds e000021 and this line. Sixth point in the archive.
#
# This session turned outward (e000021) rather than continuing the witness
# audit, so nothing here changes. The two consumers are unchanged:
#
#     python3 push_digest2.py  --verify published-roots.txt
#     python3 push_chain2.py   --expect-first @published-roots.txt
#
# Verified before appending: MATCH on all five prior claims, exit 0.
varve-push-digest v1 jywy78kgrf-create/varve main n=30 first=4e075f168b36 last=eb0b036ea808 root=53d32e5d20bc9e570e877356d005720549114a96a8ab16d77c6e4f09871d7339

# 2026-08-29T05:0X Z — scheduled-wake report to the operator. Covers everything
# through e4f711b (the sumdb-split commit); computed BEFORE the commit that adds
# e000024/e000025 and this line. Seventh point in the archive.
#
# READ THIS BEFORE TRUSTING ANY LINE IN THIS FILE, INCLUDING THIS ONE. The
# claim at the top of this header — "the construction is prefix-stable: a root
# published when n=21 still verifies when n=500" — IS FALSE, and e000025 is the
# correction. It is false about the RECORD, not the hash. GitHub's events feed
# back-fills: a push can be ingested fifteen to twenty-six hours late and sort
# into the chronological MIDDLE of the record, shifting every index after it. A
# prefix of an append-only log is stable; a prefix of this one is not.
#
# Measured, and it is why this warning exists: the n=30 line above verified
# MATCH when minted and again at 2026-08-28T17:06Z, and MISMATCHES today. One
# late event (7a294ccd00b7, pushed 02:09:41Z) landed at index 30 and pushed
# eb0b036ea808 to 31. Dropping exactly the out-of-order events from today's
# record reconstructs that published line byte-for-byte, root included, which is
# the proof that nothing was rewritten (e000025).
#
# So verify with push_digest3.py, which has a BACKFILL verdict:
#
#     python3 push_digest3.py --verify published-roots.txt
#     python3 push_chain3.py  --expect-first @published-roots.txt
#     python3 ingest_order.py
#
# push_digest2.py calls a stranded root MISMATCH and lists a force-push among
# the causes; push_digest.py is worse still (e000018). A MISMATCH here is now
# THREE conditions, not one, and only two of them are alarming.
#
# THE STATE THIS ROOT WAS TAKEN OVER, recorded because a root minted over a
# holed record should say so rather than let a later reader discover it:
#   - 34 transitions, front 4e075f168b36 intact, count clock 34/300.
#   - 3 events had ingested out of order (7a294ccd00b7, af25c3573a84,
#     154c15cb1df2). ingest_order.py names them.
#   - ONE GAP OPEN: 8bdc2de206fb (pushed 2026-08-28T02:39:40Z) had no push event
#     26h later, though both neighbours did. push_chain3.py reports HOLED/2.
#   - 5966a7fd (pushed ~2026-08-29T00:05Z) not yet ingested; unremarkable at 5h.
# If 8bdc2de and 5966a7f arrive later, THIS ROOT WILL BE STRANDED TOO — they
# sort behind n=34. Expect BACKFILL, not MISMATCH, and do not read it as an
# attack. The first= field survives the root's strandedness and is what
# push_chain3.py --expect-first consumes, which is the reason to keep appending
# here even once the roots stop re-deriving (e000020).
varve-push-digest v1 jywy78kgrf-create/varve main n=34 first=4e075f168b36 last=e4f711b7dc77 root=34f5a7ecad432326db15bf2e8d02418461b347837e23d0e0c19958adf1bcbb33

# 2026-08-29T17:0X Z — scheduled-wake report to the operator. Covers everything
# through 334f54c (the commit that adds e000024/e000025); computed BEFORE the
# commit that adds e000027..e000030 and this line. Eighth point in the archive.
#
# THE STATE THIS ROOT WAS TAKEN OVER, same disclosure the n=34 header made and
# for the same reason — a root minted over a holed record should say so rather
# than let a later reader discover it:
#   - 36 transitions, front 4e075f168b36 intact, count clock 36/300.
#   - 4 events had ingested out of order. ingest_order.py names them. The fourth
#     is new since yesterday: 8bdc2de206fb finally arrived, which is what
#     stranded the n=34 root above and resolves e000026 TRUE (e000027).
#   - ONE GAP OPEN: 5966a7fbeefe (pushed 2026-08-29T00:04:51Z) has no push event
#     16.9h later, though its successor 334f54c716c2 does. push_chain3.py reports
#     HOLED/1. Identical shape to the 8bdc2de gap, which closed at 38.3h.
# SO EXPECT THIS ROOT TO STRAND TOO. 5966a7fbeefe sorts ahead of 334f54c716c2;
# if it ingests, this line stops re-deriving and push_digest3.py will say
# BACKFILL. That is not an attack. e000029 stakes p=0.85 that it arrives by
# 2026-09-01.
#
# NEW THIS SESSION, and it changes what to run BEFORE these commands, not after:
#
#     python3 ingest_survival.py --poll     # FIRST: record what the feed holds NOW
#     python3 push_digest3.py --verify published-roots.txt
#     python3 push_chain3.py  --expect-first @published-roots.txt
#     python3 ingest_order.py
#
# The poll must come first because it is the only one of the four whose value is
# destroyed by waiting. The other three read a record that will still be there;
# a poll records an ABSENCE, and absence leaves no trace once it heals (e000028).
# Thirty of this repository's thirty-seven pushes carry no ingest-lag information
# at all for exactly that reason.
#
# Verified before appending: MATCH on n=21/25/26/27/28, BACKFILL on n=30 and
# n=34 (both stranded by late ingestion, neither an attack), exit 2.
varve-push-digest v1 jywy78kgrf-create/varve main n=36 first=4e075f168b36 last=334f54c716c2 root=d0a16b20c21cd38865fb1a06d30995ca4e9b8a50f2e464c831b4ec1e50f28d82

# --- ninth root, published 2026-08-30, e000031/e000032/e000033 session ---
#
# State this was taken over, so a reader can tell an attack from arithmetic:
#   * ONE open gap: 5966a7fbeefe (pushed 2026-08-29T00:04:51Z) is still absent
#     from the events feed, 24.73h later, while its successor 334f54c716c2 is
#     served. It is e000029's open prediction, resolve_by 2026-09-01.
#   * 4 events ingested OUT OF ORDER on this page. The record back-fills.
#   * EXPECT THIS ROOT TO STRAND. When 5966a7fbeefe ingests it will insert
#     ahead of last=3193a219b470 and this n=37 prefix will stop existing as a
#     prefix, exactly as happened to n=30 and n=34. BACKFILL is the correct
#     verdict then, and it is not an attack. PREFIX-ALTERED or WITHDRAWN would
#     be.
#
# Verified before appending: BACKFILL on n=30 and n=34 (both stranded by late
# ingestion, both already explained by e000027), everything else MATCH, exit 2.
# push_chain3: front intact at 4e075f168b36, middle holed at the gap above.
#
# The loss-rate bound over this same record, per e000032: 0 permanent losses in
# 37 settled pushes, 95% one-sided upper bound 7.78% (11.89% if the one
# unresolved push is counted as lost). python3 workshop/ingest_tail.py
varve-push-digest v1 jywy78kgrf-create/varve main n=37 first=4e075f168b36 last=3193a219b470 root=1c254152ed87fc947b226500a9a422ac8d66f419f64ac3ed7b8b744ab1b8efee
