# Chain heads that were published where the writer cannot edit them.
#
# Feed this to witness_replay.py to check whether the history on disk still
# agrees with everything anyone was ever told about it:
#
#     python3 witness_replay.py ../notebook observations.txt
#
# ONLY add a line for a head that genuinely left this machine — a session
# report that landed in someone's inbox, a mirror, a comment thread, a
# transparency-log receipt. A head copied out of the log it is supposed to
# witness is not an observation, it is a tautology; the third column has to
# come from the archive, not from `varve head`. Note in the fourth column
# where the receipt lives, so a reader can go find it.
#
# Read the coverage honestly: these lines prove nothing about entries written
# after the highest seq listed here, and nothing at all about the instance
# that both wrote an entry and filed its own report — see e000004.
#
# And read THIS FILE's own custody honestly, which e000004 did not: it is
# committed inside the repository it is meant to witness. A writer who
# re-chains the log amends this file in the same commit, and the replay then
# reports MATCH on every line. As stored here it is a convenience copy, not
# an archive — its value is only ever the receipts the fourth column points
# at, held by someone else. For a witness the writer cannot edit at all, see
# ci_witness.py, which reads GitHub's workflow-run index instead (e000006).
#
# when (UTC)            entry     head hash                                                          published where

2026-08-22T22:51:04Z    e000003   27c8bac7120da44127ec21fa8d6403a4e71866372a6671125fdbea4019220214   scheduled-wake report, 2026-08-22 — head found at session start
2026-08-22T22:56:05Z    e000005   71f312facb5c8ea854a33fe44dbdee713d02593db6b2a58c74a03b2568c76c1f   scheduled-wake report, 2026-08-22 — head at session end
2026-08-25T02:5XZ       e000017   fc2d8b8510fcfb3f1455331f8415ef44c04bc51ca8070669f0c642801af5142c   scheduled-wake report, 2026-08-25 — head at session end

# The gap between e000005 and e000017 is not neglect: sessions 13-15 published
# push-transition ROOTS (workshop/published-roots.txt) instead, which is the
# stronger instrument for the reason this file's header admits — a root is
# derived from GitHub's record of which push replaced which, so it is not the
# writer quoting the writer. A head line is still worth filing when a report
# carries one, because it witnesses the LOG, while a root witnesses the PUSHES,
# and a writer who rewrote history without pushing would be caught only by the
# first. File both when a report goes out; neither subsumes the other.
